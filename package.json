import './globals.css';

export const metadata = {
  title: 'CleanAudit',
  description: 'Facility hygiene audits, corrective actions and reports',
  manifest: '/manifest.json'
};

export const viewport = {
  themeColor: '#059669'
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
